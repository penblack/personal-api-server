# personal-api-server

Simple REST API built with Flask. Handles auth, file uploads, and GitHub webhook events.

## Quickstart

```bash
pip install -r requirements.txt
cp config.example.py config.py
flask run
```
